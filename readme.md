# Arround the corner
# Launch project
```shell
# Server
composer install
php artisan migrate

# Client
yarn install
yarn build

# Project
docker-compose up
docker-compose exec laravel php artisan migrate        
```